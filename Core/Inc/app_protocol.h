/**
  ******************************************************************************
  * @file    app_protocol.h
  * @brief   USART2 测量上报与 SETTIME（含旧版 TIME 别名）命令接口。
  ******************************************************************************
  */

#ifndef __APP_PROTOCOL_H__
#define __APP_PROTOCOL_H__

#ifdef __cplusplus
extern "C" {
#endif

#include "app_state.h"

/**
 *******************************************************************************
 * @brief  初始化协议模块内部的串口接收缓冲状态。
 *******************************************************************************
 */
void app_protocol_init(void);

/**
 *******************************************************************************
 * @brief  读取 RTC 当前快照，写回应用状态。
 * @param  app AppState 指针。
 *******************************************************************************
 */
void app_protocol_update_rtc_snapshot(AppState_t *app);

/**
 *******************************************************************************
 * @brief  轮询 USART2 命令行，处理 SETTIME 及旧版 TIME 设置别名。
 * @param  app AppState 指针。
 * @note   命令接收来自 DMA 环形缓冲；本函数只处理已到达的完整行。
 *         推荐使用 SETTIME；TIME 必须携带相同负载，不表示无参数查询。
 *******************************************************************************
 */
void app_protocol_poll_uart_commands(AppState_t *app);

/**
 *******************************************************************************
 * @brief  读取 AppState 一致快照，组包并通过串口上报当前测量数据。
 * @param  app AppState 指针。
 * @note   M 帧遵循 append-only 字段契约，当前由尾部 field_count 校验列数。
 *******************************************************************************
 */
void app_protocol_send_sensor_report(AppState_t *app);

#ifdef __cplusplus
}
#endif

#endif /* __APP_PROTOCOL_H__ */
